// require keyword necessary for importing modules
var movies = require("./movies");

movies.avatar;

movies.printOcean();

// by default the same instance of modules is shared between all objects